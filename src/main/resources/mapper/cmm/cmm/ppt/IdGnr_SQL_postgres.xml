<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BrewIdGnrDAO">

    <!-- FILE_ID -->
    <select id="getNextFileId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
            where 1=1
                and table_name = 'FILE_ID'
    </select>

    <update id="updateNextFileId" parameterType="int">
        UPDATE brew.comtecopseq
            SET next_id = next_id + 1
        WHERE table_name = 'FILE_ID'
    </update>

    <!-- LOG_ID -->
    <select id="getNextLogId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'LOG_ID'
    </select>

    <update id="updateNextLogId" parameterType="int">
        UPDATE brew.comtecopseq
            SET next_id = next_id + 1
        WHERE table_name = 'LOG_ID'
    </update>

    <!-- LOGIN_ID -->
    <select id="getNextLoginLogId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'LGI_ID'
    </select>

    <update id="updateNextLoginLogId" parameterType="int">
        UPDATE brew.comtecopseq
        SET next_id = next_id + 1
        WHERE table_name = 'LGI_ID'
    </update>

    <select id="getNextBbsId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'BBS_ID'
    </select>

    <update id="updateNextBbsId" parameterType="int">
        UPDATE brew.comtecopseq
        SET next_id = next_id + 1
        WHERE table_name = 'BBS_ID'
    </update>

    <select id="getNextFaqId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'FAQ_ID'
    </select>

    <update id="updateNextFaqId" parameterType="int">
        UPDATE brew.comtecopseq
        SET next_id = next_id + 1
        WHERE table_name = 'FAQ_ID'
    </update>

    <select id="getNextCodeId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'CODE_ID'
    </select>

    <update id="updateNextCodeId" parameterType="int">
        UPDATE brew.comtecopseq
        SET next_id = next_id + 1
        WHERE table_name = 'CODE_ID'
    </update>

    <select id="getNextUserId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'USER_ID'
    </select>

    <update id="updateNextUserId" parameterType="int">
        UPDATE brew.comtecopseq
        SET next_id = next_id + 1
        WHERE table_name = 'USER_ID'
    </update>

    <select id="getNextExitId" resultType="int">
        SELECT next_id
        from brew.comtecopseq
        where 1=1
          and table_name = 'EXIT_ID'
    </select>

    <update id="updateNextExitId" parameterType="int">
        UPDATE brew.comtecopseq
        SET next_id = next_id + 1
        WHERE table_name = 'EXIT_ID'
    </update>
</mapper>